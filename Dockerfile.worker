FROM node:20-alpine
WORKDIR /app

RUN apk add --no-cache poppler-utils

COPY package.json package-lock.json* ./
RUN npm ci --ignore-scripts

COPY . .

CMD ["npx", "tsx", "scripts/worker-entrypoint.ts"]
